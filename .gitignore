# Secrets — never commit
.env

# state/ and out/ ARE tracked: sessions run in ephemeral containers, so append-only
# state and delivered digests survive only if pushed. See CLAUDE.md working conventions.
